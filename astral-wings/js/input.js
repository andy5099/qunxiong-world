export function input(canvas){const s={x:180,y:555,drag:false,keys:{}};const pos=e=>{const r=canvas.getBoundingClientRect();return{x:(e.clientX-r.left)*360/r.width,y:(e.clientY-r.top)*640/r.height}};canvas.addEventListener('pointerdown',e=>{if(s.drag)return;s.drag=true;canvas.setPointerCapture(e.pointerId);Object.assign(s,pos(e))});canvas.addEventListener('pointermove',e=>{if(s.drag)Object.assign(s,pos(e))});canvas.addEventListener('pointerup',()=>s.drag=false);addEventListener('keydown',e=>s.keys[e.key.toLowerCase()]=true);addEventListener('keyup',e=>s.keys[e.key.toLowerCase()]=false);return s}
