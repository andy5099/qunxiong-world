# V0.2.2 三選項驗證

2026-09-24：48 組 Node 測試通過，執行 `node --test dream-world/tests/*.test.js`。

- 新回應必須三個不同 label／intent；重複場景、整組舊選項、停滯聊天拒絕提交，保留原狀態。
- 舊版 0–2 選項存檔仍可載入並接續；自訂行動預設折疊。
- 自動測試連續選擇 20 回合，每次驗證實際 intent、不同場景與選項、存檔重載；第 21 回合仍可續玩。
- 本機 HTTP 模擬端點 test-adventure：瀏覽器實際依序輪流點 ①②③ 共 20 次。每次出現新場景與三個新選項，第 21 幕重新整理後保留，重要記憶僅 4 個里程碑，普通對話不進 ledger。
- 重新整理後重新填入本機測試連線，點選成功前進到第 22 幕。390×844 畫面無橫向溢出，三個按鈕高度約 104px，自訂輸入預設收合。真機 Safari 未驗收。
- 保留 OpenRouter／Dolphin 請求組裝、模型切換、金鑰不序列化、402／429 不自動切離線，以及原多世界／外掛／角色界線測試。

上述 20 回合使用明確標示的固定測試資料，僅驗證 UI、HTTP、JSON、狀態交易與保存流程，**不是 20 回合真實 AI 品質驗收**。本次未使用任何真實模型推論，Dolphin 的語意多樣性、NPC 主動性及長篇連貫性尚需可用帳戶金鑰與額度驗收。沒有部署免設定的登入／限額後端。測試 fixture 不作為正式遊戲的 AI fallback，亦不加入 Service Worker 快取。

## 以下為歷史版本紀錄（舊操作規則已由 V0.2.2 取代）

# V0.2.1 自由對話驗證

2026-09-24：46 組 Node 測試通過。執行 `node --test dream-world/tests/*.test.js`。

新增與更新驗證：

- AI 開場及主畫面的輸入框直接顯示，沒有 details 折疊；0／1／2／3 個快捷行動皆合法，重複或超過 3 個拒絕。
- 玩家話語原文送入 context；連續 15 輪 dialogue 且 choices=[] 仍可保存、摘要與重載，不強迫新事件。
- 已有三選項的 V0.2 存檔與 V0.1／V0.1.1 遷移測試通過；全部原有世界／外掛玩法測試保留。
- OpenRouter 指定 Base URL、模型 ID 的請求組裝、模型切換、端點切換清除金鑰、存檔與 UI 不含測試 Key。
- 402／429 保留原狀態與 AI 模式，不自動降為離線；逾時、壞 JSON、重寫與界線檢查沿用。

手機尺寸 Chromium 實際操作（390×844）：

1. 新開場直接輸入；無金鑰送出顯示 AI 暫不可用，輸入草稿原文仍在。
2. Provider 選 OpenRouter，自動帶入 https://openrouter.ai/api/v1 和 cognitivecomputations/dolphin-mistral-24b-venice-edition。
3. 改用本機模擬端點 4195、Model test-dialogue：連續兩次自由输入後回覆 choices=[]，始終有輸入框，回合正常增加。
4. 切 Model test-error，得到 429；仍是原第 3 幕／修為 4／EXP 10／能量 48，未切離線，待送文字保留。
5. 重新載入後已成功對話及零快捷選項保留，輸入框可見；未提交草稿與金鑰只在分頁記憶體，不隨重新整理保留。
6. 畫面 DOM 檢查無橫向溢出。真機 Safari 未驗收。

實際付費模型推論：**無**。沒有可用金鑰或額度；僅以 OpenRouter 官方公開端點確認模型及 response_format 支援，再用模擬 transport／本機 HTTP 端點驗證流程。未測試指定模型的真實對話品質或成人內容表現。

未部署含登入、配額及伺服器金鑰的後端；不能宣稱免設定 AI。非露骨、成年、自願與不可清除已表明界線的限制保留，未實作解除非露骨限制；不強制固定淡出或制式台詞。

## 以下為 V0.2 歷史驗證紀錄

# V0.2 驗證紀錄

日期：2026-09-24。42 組 Node 測試通過（原有 26 + AI 16）。

```sh
node --test dream-world/tests/*.test.js
```

## AI 自動測試

- 完整 response schema、3 個不同 label／intent、未成年／原型鍵／未知欄位／數值界限拒絕。
- 真正本機 HTTP roundtrip：Authorization、JSON mode、messages、回應解析及測試連線。
- malformed JSON、空結果、截斷、401／429／500 及 timeout／AbortSignal：來源狀態不變。
- 生成與重寫交易：只回傳新狀態，不突變來源。匯出重載後連續重寫仍無重複 XP、修為、資源、NPC 或重要記憶。
- AI／離線切換後可繼續；AI 下一幕沒有固定 next scene。
- AI 新 NPC（含秘密與界線）、地點、任務、伏筆、物品、勢力、秘密完整存檔往返與 context。
- 120 幕自動摘要後保留最近 12 幕、12 份舊幕摘要、最早承諾及未完成任務，121 個永久事實（含敵人）與早期特殊物品均保留。
- V0.1.1 migration 比對原數值／記憶；V0.1 真實 fixture 沿用。另一個世界與其他 storage key 不變。
- 親密條件、拒絕不可撤銷、朋友界線、同意後戀愛成長、未知效果／未解鎖能力拒絕。
- AI abilityAction 真正扣能量與冷卻，重写不重複扣款。
- stagnationScore 達門檻時拒絕無推進對話。
- SessionCredentials 不可 JSON 序列化出金鑰、端點變更清除舊 Key、拒絕非 HTTPS 遠端／URL 夾帶認證。
- 巢狀檢查點拒絕，生成文字 HTML escaping，Service Worker 模組全量完整與隔離。

原有 160 路線 × 40 回合（6,400 次選擇）及 7 世界 × 4 外掛 × 18 回合（504 次選擇）仍全部執行。

## 瀏覽器操作驗證

本機 `4190` 遊戲搭配 `4192` 模擬 Provider，回應明確標示「本機模擬，非真正 AI」。使用桌面 Chromium 的手機 viewport。

1. 新存檔預設 AI，設定頁含 Provider／Model／Base URL／password Key／Creativity／連線測試。
2. 真正透過瀏覽器 HTTP 連至本機端點，顯示連線成功。測試發現並修正瀏覽器 fetch 的呼叫綁定問題。
3. 生成開場出現 3 選項；點第 1 項後第 2 幕、修為 4、EXP 5、能量 44。
4. 重寫後仍是第 2 幕、修為 4、EXP 5、能量 44；AI 新人「藍笙」出現在角色名冊且只有一位。
5. 改 Model 為 test-malformed，點選後顯示 JSON 錯誤、第 2 幕與資源不變，提供重新生成／切換離線。
6. 修正 Model 後重試，進入第 3 幕、修為 6、EXP 10、能量 48，只結算一次。
7. 自訂行動送出後進入第 4 幕，回到 3 選項，沒有聊天 UI。
8. UI 匯出 JSON：含新 NPC、4 幕記憶、檢查點；DOM 中的匯出文字不含測試 Token 或 Provider URL。
9. 切離線後數值保留，原模板三選項可玩；AI 世界資料保留。
10. 重新開啟分頁保留第 4 幕、修為 8、EXP 15、能量 52；設定回到預設模型且金鑰欄為空。375px 畫面 documentWidth 與 clientWidth 同為 360（扣除捲軸），沒有橫向溢出；實際截圖檢查表單正常。

## 尚未驗收

- 無私人 API 金鑰／額度，未做真實 OpenAI 或第三方模型推論，也未主張通過。支援 OpenAI gpt-4.1-mini 的 HTTP 協定依官方文件實作；使用者需以自己的帳戶按「測試連線」及生成第一幕。
- 未部署私人 Proxy；本機範例只適合個人使用。
- 未提供 iPhone Safari 真機／PWA 安裝環境，未驗收；手機尺寸 Chromium 不等同真機。
- AI 劇情品質與自然語言是否完全遵循提示需真實模型遊玩評估；程式驗證只對結構與機制負責。

## V0.1.1 歷史紀錄（以下是當時版本結果）

# V0.1.1 驗證紀錄

日期：2026-09-24。26 組 Node 測試通過；桌面 Chromium 內嵌瀏覽器，390×844 與 375×812 viewport。

```sh
node --test dream-world/tests/*.test.js
```

保留原有 160 條路線 × 40 回合（6,400 次選擇），另驗證 7 種世界 × 4 種外掛 × 18 回合（504 次選擇）；每回合三個可用選項，並反覆完成存檔格式往返。

## 需求對照

| # | 項目 | 結果／證據 |
|---|---|---|
| 1 | V0.1 migration | 原始 playable-save.json 未覆蓋。純函式比對原數值、關係、記憶、sceneId；UI 匯入後保留第 11 幕、煉氣二層，補上 Lv.3 太虛系統 |
| 2 | 第二世界 | UI 五步建立《哥布林異世界》，加入沈清霜、族群繁榮系統；生成首幕與三選項 |
| 3 | 第三世界 | UI 建立《末日安全屋》，無角色，自訂「夜航安全屋」及「燈火能量」 |
| 4 | 切換 | UI 切回太虛，仍是第 2 幕、EXP 7、能量 34 |
| 5 | 進度隔離 | 自動測試比對整個未啟用世界快照；UI 看到三份不同進度與外掛 |
| 6 | 刷新 | 第三世界使用安全屋後刷新，物資 13、基地 1、能量 32 均保留；世界頁仍有三個世界 |
| 7 | 刪除 | UI 先顯示指定名稱與二次確認；刪除哥布林世界後，太虛與末日世界保留 |
| 8 | 自訂世界 | 自動測試玻璃海：名稱、背景、規則、身份、數值、外掛、角色匯出／匯入保留 |
| 9 | 外掛獨立 | 全部 28 種世界×外掛组合可執行；UI 同時保存三種不同外掛 |
| 10 | 外掛升級 | EXP 測試通過，Lv.1–5 門檻可達成；舊存檔按回合數補 EXP |
| 11 | 能力解鎖 | 初始只有第一項能力；不同 EXP 解鎖第二／第三／第四能力，未達門檻不可執行 |
| 12 | 改變 123 | 首幕第三選項為仙緣之眼；新世界第三選項依模板生成族群招募／安全屋，冷卻時回復一般選項 |
| 13 | 太虛開局 | UI 顯示煉氣一層→叮／太虛覺醒→仙緣之眼→高契合成年角色 |
| 14 | 仙緣之眼 | UI 顯示沈清霜 26 歲、化神初期、關係／好感／信任／契合 90%；能量 40→34，冷卻生效 |
| 15 | 太虛夢境 | 自動驗證低信任拒絕／高信任接受；拒絕不給親密，成年與界線門檻有效 |
| 16 | 系統任務 | UI 共鳴後顯示任務完成 +20 能量、+10 EXP；自動測試不重複領獎 |
| 17 | 關係數值 | UI 共鳴 +3 好感／+3 信任／+3 親密，角色成長 +7；三數值獨立 |
| 18 | 對話等級 | 自動測試 0→1→2→3→4 的門檻；UI 舊存檔相處台詞為 Lv.2 |
| 19 | 三角色風格 | 每級三人台詞互不相同；清霜嘴硬、媚璃主動、傾城克制 |
| 20 | intimacy 分層 | moon／dialogue／events 位於 data/intimacy；引擎只判定與結算 |
| 21 | 自訂行動 | 不授予虛構進度或外掛 EXP，確認後返回原 sceneId 和三選項 |
| 22 | 375／390 | 實際截图檢查世界建立、首幕、管理頁；375 viewport 的 documentWidth 360（扣除捲軸），沒有橫向溢出 |
| 23 | iPhone Safari | 未提供 iOS 真機／Safari 測試環境，未驗收。已使用 safe-area、直式排版、16px 表單字體及原生表單，不能等同真機通過 |
| 24 | 匯出／匯入 | UI 下载 dream-world-worlds.json，解析確認含三世界與自訂外掛；UI 匯入舊 JSON 正常；新格式完整往返測試通過 |
| 25 | lineage-text 隔離 | Git 路徑檢查只含 dream-world/；storage namespace 不變，service worker 拒絕接管 lineage-text 请求 |

額外驗證：損壞／重複世界存檔原子拒絕、原型鍵拒絕、使用者 HTML 字串 escaping、所有選取角色都可相遇（不需要仙緣之眼）、共鳴雙方獲益／體力消耗／仙元煉化、朋友界線及未成年拒絕、全量 service worker 模組路徑存在。

## 可重現流程

1. `node dream-world/tools/serve.js 4181`，開啟 localhost 的 `/dream-world/`。
2. 開始太虛，選第三項仙緣之眼，檢查資訊與資源。
3. 世界頁依五步流程建立奇幻世界，使用族群招募。
4. 再建立末日世界，自訂安全屋外掛，不勾角色，使用安全屋。
5. 刷新、切回太虛，再匯出全部世界；刪除中間那個世界，確認其他世界保留。
6. 在本機測試來源匯入原始 `tests/playable-save.json`（会取代目前全部世界，先備份），檢查回合／關係／外掛。
7. 外掛面板選沈清霜與仙緣共鳴，經三選項確認後檢查修為、角色成長、仙元與任務。

## 邊界

- 沒有 AI／圖片／影片 API；不聲稱自由世界描述會生成任意完整劇本。
- 沒有圖片／影片實際素材，沿用 none 降級與接口測試。
- 未做 iPhone Safari、PWA 安裝與飛航模式真機驗收。
- 其他既有遊戲以檔案、存檔與 service worker scope 隔離驗證，沒有為此開啟或更改其存檔。
