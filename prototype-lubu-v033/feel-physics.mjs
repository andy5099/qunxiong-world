export const FEEL_PRESETS={
 balanced:{gravity:545,normalLaunch:690,perfectLaunch:810,maxSpeed:920,dashSpeed:790,dashCooldown:.75,bossBounceRetention:.9,dashBossRetention:.96,wallBounceRetention:.92,ceilingRetention:.88,airDrag:.998,followerDelay:[.08,.14],flipSide:150},
 highBounce:{gravity:510,normalLaunch:760,perfectLaunch:910,maxSpeed:1060,dashSpeed:900,dashCooldown:.68,bossBounceRetention:.94,dashBossRetention:.98,wallBounceRetention:.95,ceilingRetention:.9,airDrag:.9985,followerDelay:[.08,.14],flipSide:175},
 extreme:{gravity:470,normalLaunch:870,perfectLaunch:1040,maxSpeed:1240,dashSpeed:1080,dashCooldown:.55,bossBounceRetention:.97,dashBossRetention:.99,wallBounceRetention:.975,ceilingRetention:.93,airDrag:.999,followerDelay:[.065,.12],flipSide:205}
};
export function simulateFeelPreset(config,flips=30,perfectEvery=4){const runs=[];for(let n=0;n<flips;n++){const perfect=n%perfectEvery===0;let y=650,vy=-(perfect?config.perfectLaunch:config.normalLaunch),x=180,vx=(n%2?1:-1)*config.flipSide,peak=y,boss=0,wall=0,speedSum=0,frames=0,bottomAt=0,heat=[0,0,0];for(let t=0;t<8;t+=1/120){vy+=config.gravity/120;vx*=Math.pow(config.airDrag,.5);vy*=Math.pow(config.airDrag,.5);x+=vx/120;y+=vy/120;const speed=Math.hypot(vx,vy);speedSum+=speed;frames++;peak=Math.min(peak,y);heat[y<280?0:y<505?1:2]++;if(x<20||x>340){x=Math.max(20,Math.min(340,x));vx=-vx*config.wallBounceRetention;wall++}if(y<35){y=35;vy=Math.abs(vy)*config.ceilingRetention}if(y>680){bottomAt=t;break}if(Math.hypot(x-180,y-205)<70){boss++;vy=Math.abs(vy)*config.bossBounceRetention;vx+=(x<180?-1:1)*120}}runs.push({perfect,peak,boss,wall,bottomAt,avgSpeed:speedSum/frames,heat})}const avg=k=>runs.reduce((s,r)=>s+r[k],0)/runs.length,kindAvg=perfect=>{const selected=runs.filter(r=>r.perfect===perfect);return selected.reduce((s,r)=>s+r.peak,0)/selected.length},heat=runs.reduce((a,r)=>a.map((v,i)=>v+r.heat[i]),[0,0,0]),total=heat.reduce((a,b)=>a+b,0);return{flips,highestY:Math.min(...runs.map(r=>r.peak)),averageHighestY:avg('peak'),normalAverageHighestY:kindAvg(false),perfectAverageHighestY:kindAvg(true),normalAverageRise:650-kindAvg(false),perfectAverageRise:650-kindAvg(true),bossHits:runs.reduce((s,r)=>s+r.boss,0),wallHits:runs.reduce((s,r)=>s+r.wall,0),averageReturn:avg('bottomAt'),averageSpeed:avg('avgSpeed'),heatmap:{upper:heat[0]/total,middle:heat[1]/total,lower:heat[2]/total},bossHeightReach:runs.filter(r=>r.peak<=250).length/flips}}
