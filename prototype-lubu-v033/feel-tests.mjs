import assert from 'node:assert/strict';import{FEEL_PRESETS,simulateFeelPreset}from'./feel-physics.mjs';const results=Object.fromEntries(Object.entries(FEEL_PRESETS).map(([k,v])=>[k,simulateFeelPreset(v)]));assert.ok(results.highBounce.bossHeightReach>.5);assert.ok(results.highBounce.averageHighestY<250);assert.ok(results.highBounce.heatmap.lower<.65);assert.ok(results.extreme.averageHighestY<results.highBounce.averageHighestY);assert.equal(FEEL_PRESETS.highBounce.followerDelay[0],.08);assert.equal(FEEL_PRESETS.highBounce.followerDelay[1],.14);console.log(JSON.stringify(results,null,2));
