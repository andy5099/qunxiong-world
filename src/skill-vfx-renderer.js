const MAX_PARTICLES=96,particles=Array.from({length:MAX_PARTICLES},()=>({active:false}));
const colors={slash:'#66f3ca',smash:'#ffb24e',support:'#ffe58a',ricochet:'#a58cff',burn:'#ff6338',pierce:'#8fbdff'};
function particle(x,y,color,angle,speed,life=.34){const p=particles.find(item=>!item.active);if(!p)return;p.active=true;p.x=x;p.y=y;p.vx=Math.cos(angle)*speed;p.vy=Math.sin(angle)*speed;p.color=color;p.life=p.maxLife=life;}
function seed(effect){if(effect.vfxSeeded)return;effect.vfxSeeded=true;const key=effect.type?.replace('skill-','')||'slash',color=colors[key]||'#ffe58a',count=effect.type==='ultimate'?28:effect.type?.startsWith('bond-')?22:14;for(let i=0;i<count;i++)particle(effect.x,effect.y,color,Math.PI*2*i/count+(i%3)*.13,45+(i%5)*22,effect.type==='ultimate'?.62:.36);}
export function renderSkillVfx(ctx,effect,dt=.025){if(!effect?.type||(!effect.type.startsWith('skill-')&&effect.type!=='ultimate'&&!effect.type.startsWith('bond-')&&!effect.type.startsWith('talent-')))return false;seed(effect);const p=1-Math.max(0,Math.min(1,effect.life/(effect.maxLife||1))),kind=effect.type.replace('skill-',''),color=colors[kind]||(effect.type.includes('fire')?'#ff6338':'#ffe58a');ctx.save();ctx.translate(effect.x,effect.y);ctx.globalAlpha=Math.max(0,Math.min(1,effect.life));ctx.strokeStyle=color;ctx.fillStyle=color;if(kind==='slash'){ctx.lineWidth=8;for(let i=0;i<3;i++){ctx.beginPath();ctx.arc(0,0,28+i*16,-2.5+p*1.5,-.2+p*1.5);ctx.stroke();}}else if(kind==='smash'){ctx.lineWidth=7;ctx.beginPath();ctx.arc(0,0,22+p*76,0,Math.PI*2);ctx.stroke();for(let i=0;i<8;i++){ctx.rotate(Math.PI/4);ctx.fillRect(18+p*35,-3,20,6);}}else if(kind==='support'){ctx.lineWidth=5;ctx.beginPath();ctx.arc(0,0,30+p*60,0,Math.PI*2);ctx.stroke();ctx.rotate(p);for(let i=0;i<8;i++){ctx.rotate(Math.PI/4);ctx.fillRect(46,-2,12,4);}}else if(kind==='ricochet'){ctx.lineWidth=7;ctx.beginPath();for(let i=0;i<18;i++){const a=i/17*Math.PI*3,r=10+i*4;ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r);}ctx.stroke();}else if(kind==='burn'||effect.type.includes('fire')){ctx.lineWidth=9;for(let i=0;i<5;i++){ctx.beginPath();ctx.moveTo(-45+i*20,35);ctx.quadraticCurveTo(-32+i*18,-20-p*30,-16+i*17,-48);ctx.stroke();}}else{ctx.lineWidth=6;ctx.beginPath();ctx.arc(0,0,24+p*72,0,Math.PI*2);ctx.stroke();}ctx.restore();for(const item of particles)if(item.active){item.life-=dt;if(item.life<=0){item.active=false;continue;}item.x+=item.vx*dt;item.y+=item.vy*dt;ctx.save();ctx.globalAlpha=item.life/item.maxLife;ctx.fillStyle=item.color;ctx.beginPath();ctx.arc(item.x,item.y,2.2,0,Math.PI*2);ctx.fill();ctx.restore();}return true;}
export function getSkillParticleCount(){return particles.filter(item=>item.active).length;}
